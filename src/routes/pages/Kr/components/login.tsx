


// 分为两个渠道
// 1. 输入邀请码后，存到本地，并跳转去链接x，跳转回后
// 2. 如果已经是老用户，则直接链接他的x

// 注意，强制拦截，需要处理auth
export const LoginKrActivity = () => {
    return (
        <div>
            <h1>Login Kr Activity</h1>
        </div>
    );
};